#include <rex/hook.h>

// REX_STUB(sub_8227A9F0); // this is the press start screen 
// REX_STUB(sub_8227B218); // this is the fmv loader
// REX_STUB(sub_823B2E98); // breaks game after rare fmv
// REX_STUB(sub_823B3598); // breaks game after rare fmv
// REX_STUB(sub_823B3CE0); // breaks game after rare fmv
// REX_STUB(sub_823BCB00); // breaks game after rare fmv
//  REX_STUB(sub_823ED2A8); // breaks game

//REX_STUB(sub_82258AF8); //probably models
//REX_STUB(sub_821FF2A0); // seems to be grass
//REX_STUB(sub_82204F18);
//REX_STUB(sub_82235E68);
//REX_STUB(sub_82251F70);